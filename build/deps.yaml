steps:
 - name: gcr.io/kaniko-project/executor:latest
   args:
   - --dockerfile=build/kernel.Dockerfile
   - --context=/workspace
   - --destination=us-docker.pkg.dev/ajackura-dev/ecl/kernel:latest
   - --cache=true
   - --cache-ttl=12h
options:
  machineType: 'E2_HIGHCPU_8'